# Deployment Status

Date: 2026-07-31

## Master profile validated baseline

- 30 of 30 master-lab containers running.
- 18 Cisco XRd nodes running IOS XR 24.2.11.
- 11 Cisco IOL nodes running IOS XE Dublin 17.12.1.
- One Ubuntu 24.04 automation workstation, `AUTO1`, at `10.201.255.150`.
- The original 39 directly connected links passed IPv4 and IPv6 validation.
- Eight additional provider links, `L040-L047`, extend the core through P7/P8
  and dual-home PE7/PE8.
- Zero container restarts.
- Zero OOM events.
- Zero failed systemd units.

## Baselines applied

- `00-base`: dual-stack loopbacks and point-to-point addressing.
- `10-isis`: dual-stack Level-2 IS-IS on P, PE and RR/PCE nodes.
- `15-provider-standard`: banner, standardized provider IPv6 plan, IS-IS
  single topology, LFA/FRR, MPLS TE extensions and SR-TE hierarchy.
- `20-sr-mpls`: SRGB `16000-23999`, IPv4 Prefix-SIDs `16001-16018` and
  IPv6 Prefix-SIDs `16601-16618`.
- `30-bgp-rr` foundation: PE7 and PE8 have established VPNv4 and VPNv6
  sessions to both RR1 and RR2.

## Control-plane validation

- Expected IS-IS adjacencies for P7, P8, PE7 and PE8 are Up.
- IPv4 and IPv6 loopback reachability from the four new routers to P1 passed.
- IPv4 SR labels `16015-16018` and IPv6 SR labels `16615-16618` are installed.
- PE7 and PE8 have redundant VPNv4/VPNv6 control-plane sessions to RR1/RR2.
- MPLS traceroute from P1 to RR2 used label `16014`.
- IPv6 SR label `16614` is installed for RR2.

## Automation validation

- Python 3.12.3.
- Ansible Core 2.21.2 with IOS, IOS XR, NSO and supporting collections.
- Cisco pyATS and Genie 26.6.
- Netmiko 4.7.0, Nornir, Scrapli, ncclient and pyGNMI.
- The original 26-node management SSH/CLI validation passed.
- Netmiko from `AUTO1` to P1 passed.
- The Ansible provider pre-check against P1 completed with zero failures.
- Windows reachability to `10.201.255.150`: ping 3/3 and TCP/22 open.

## Platform findings

- XRd 24.2.11 rejects reuse of one nodal Prefix-SID index by IPv4 and IPv6;
  IPv6 therefore uses indexes `601-614`.
- XRd Control Plane accepts IS-IS BFD commands but does not instantiate BFD
  sessions on these virtual links. The active baseline omits BFD and keeps
  LFA/FRR; BFD practice should use IOL, XRv9k or physical IOS XR.
- Pre-change running configurations for the original 14 XRd nodes are stored under
  `artifacts/backups/20260728T235747Z-before-ipv6-standard`.

## Host resources with the lab running

- 60 GiB total RAM.
- Approximately 43 GiB used after the 30-node expansion stabilized.
- Approximately 17 GiB available.
- Swap usage remained at zero.
- 158 GB free on `/srv/netlab`.
- `AUTO1` uses approximately 8 MiB RAM while idle; its image is about 1.01 GB.

## Remaining acceptance work

- Rebuild `ccie-sp-automation:1.0` while no heavy profile is running so AUTO1
  contains the `/opt/venv` Ansible/Python toolchain declared by
  `automation/Dockerfile`. Building the full pyATS/Genie image alongside 18
  XRd nodes exceeded the host load gate and was stopped without affecting the
  30 running lab containers.
- Repeat the full 30-node management precheck from the rebuilt AUTO1 image.
- Implement and validate AAA with local fallback and the RPKI lab services.
- Implement the service phases for Inter-AS Options A, B and C independently,
  preserving the validated underlay and BGP baseline between exercises.
- Execute the generated one-node SRv6 parser/commit canary, then classify XRd
  support before the three-node control-plane profile. Only one heavy profile
  may run at a time.

## SRv6 capability readiness

- Three-node P1-P2-PE1 Source of Truth and generated topology are available.
- Management network `10.203.255.0/24` is isolated from Master and Inter-AS.
- XRd image ID, KVM nesting, host resources and Docker storage passed readiness.
- The P1-only parser, commit, rollback and restart canary passed. The next gate
  is the three-node IPv6/IS-IS control plane.
- No End, End.X, End.DT4, End.DT6, SRv6-TE, uSID or TI-LFA support is claimed.

## Inter-AS profile validated baseline

- Runnable 23-node topology generated by `tools/build_inter_as.py`.
- 23 of 23 nodes reached running state.
- All 70 directional IPv4/IPv6 directly connected tests passed.
- AS500: ten IS-IS links and 16 of 16 loopback reachability tests passed.
- AS65100 and AS65200: OSPFv2 and OSPFv3 adjacency counts matched inventory;
  all eight loopback tests passed in each domain.
- RR-based iBGP reached 6/6, 4/4 and 4/4 sessions per VPN address family.
- Inter-AS eBGP reached 10/10 IPv4 and 10/10 IPv6 endpoints.
- XRd/IOL link-apply, initial-dialog, OSPF and BGP findings are recorded in
  [`profiles/inter-as/TROUBLESHOOTING.md`](profiles/inter-as/TROUBLESHOOTING.md).
